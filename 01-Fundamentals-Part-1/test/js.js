const passwords = [
    "123456",
    "password",
    "admin",
    "freecodecamp",
    "mypassword123",
];

let passwordLength = passwords.filter(pass => pass.length >= 9);
console.log(passwordLength);

let js = 'fun';

if (js === 'fun') {
    alert('I will be successful!');
}